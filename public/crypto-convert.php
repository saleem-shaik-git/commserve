<?php
require_once dirname(__DIR__).'/app/helpers.php';require_auth();require_once dirname(__DIR__).'/app/Services/CryptoService.php';
$svc=new CryptoService(Database::connection());$user=auth_user();$wallets=$svc->wallets((int)$user['id']);$error=null;$success=null;$result=null;
if($_SERVER['REQUEST_METHOD']==='POST'){try{verify_csrf();$result=$svc->convert((int)$user['id'],(string)($_POST['from']??''),(string)($_POST['to']??''),(string)($_POST['amount']??''),(string)($_POST['pin']??''));$success='Conversion completed. '.$result['amount'].' '.$result['symbol'].' credited. Reference: '.$result['reference'];}catch(Throwable $e){$error=$e->getMessage();}}
$pageTitle='Convert Crypto';$currentPage='crypto';require __DIR__.'/partials/header.php';require __DIR__.'/partials/sidebar.php';
?>
<div class="row justify-content-center"><div class="col-lg-7"><div class="card border-0 shadow-sm"><div class="card-body p-4"><h3 class="fw-bold">Convert Crypto</h3><p class="text-muted">Exchange one simulated asset for another using the demo USD reference rates configured by the bank.</p><div class="alert alert-info small">Rates are demo rates, not live market prices. No external exchange or blockchain is contacted.</div><?php if($success):?><div class="alert alert-success"><?=e($success)?></div><?php endif;?><?php if($error):?><div class="alert alert-danger"><?=e($error)?></div><?php endif;?><form method="post"><?=csrf_field()?><div class="row g-3"><div class="col-md-6"><label class="form-label">From</label><select name="from" class="form-select"><?php foreach($wallets as $w):?><option value="<?=e($w['symbol'])?>"><?=e($w['symbol'])?> — <?=e(rtrim(rtrim(number_format((float)$w['balance'],18,'.',''),'0'),'.'))?></option><?php endforeach;?></select></div><div class="col-md-6"><label class="form-label">To</label><select name="to" class="form-select"><?php foreach($wallets as $w):?><option value="<?=e($w['symbol'])?>"><?=e($w['symbol'])?></option><?php endforeach;?></select></div></div><div class="mb-3 mt-3"><label class="form-label">Amount</label><input name="amount" class="form-control" inputmode="decimal" required placeholder="0.01"></div><div class="mb-4"><label class="form-label">Transaction PIN</label><input name="pin" type="password" class="form-control" required></div><button class="btn btn-primary w-100">Convert</button></form><a href="/commserve/public/crypto.php" class="btn btn-link mt-3">Back to Crypto Wallet</a></div></div></div></div>
<?php require __DIR__.'/partials/footer.php';?>