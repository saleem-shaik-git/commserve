<?php
require_once dirname(__DIR__).'/app/helpers.php';require_auth();require_once dirname(__DIR__).'/app/Services/CryptoService.php';
$svc=new CryptoService(Database::connection());$user=auth_user();$wallets=$svc->wallets((int)$user['id']);$error=null;$success=null;
if($_SERVER['REQUEST_METHOD']==='POST'){try{verify_csrf();$ref=$svc->receiveDemo((int)$user['id'],(string)($_POST['symbol']??''),(string)($_POST['amount']??''));$success='Simulated crypto receipt credited. Reference: '.$ref;}catch(Throwable $e){$error=$e->getMessage();}}
$pageTitle='Receive Crypto';$currentPage='crypto';require __DIR__.'/partials/header.php';require __DIR__.'/partials/sidebar.php';
?>
<div class="row g-4"><div class="col-lg-7"><div class="card border-0 shadow-sm"><div class="card-body p-4"><h3 class="fw-bold">Receive Crypto</h3><p class="text-muted">Each supported asset has a unique simulated deposit address.</p><?php if($success):?><div class="alert alert-success"><?=e($success)?></div><?php endif;?><?php if($error):?><div class="alert alert-danger"><?=e($error)?></div><?php endif;?><div class="row g-3"><?php foreach($wallets as $w):?><div class="col-md-6"><div class="border rounded p-3 h-100"><div class="fw-bold"><?=e($w['name'])?> (<?=e($w['symbol'])?>)</div><div class="small text-muted text-break mt-2"><?=e($w['address'])?></div><button class="btn btn-sm btn-outline-secondary mt-3" onclick="navigator.clipboard.writeText('<?=e($w['address'])?>')">Copy address</button></div></div><?php endforeach;?></div></div></div></div><div class="col-lg-5"><div class="card border-0 shadow-sm"><div class="card-body p-4"><h5 class="fw-bold">Demo funding</h5><p class="small text-muted">Use this only for testing. It creates an internal simulated receive ledger entry; it does not represent an on-chain deposit.</p><form method="post"><?=csrf_field()?><div class="mb-3"><label class="form-label">Asset</label><select name="symbol" class="form-select"><?php foreach($wallets as $w):?><option value="<?=e($w['symbol'])?>"><?=e($w['symbol'])?></option><?php endforeach;?></select></div><div class="mb-3"><label class="form-label">Amount</label><input name="amount" class="form-control" required placeholder="0.01"></div><button class="btn btn-primary w-100">Simulate Receive</button></form></div></div></div></div>
<?php require __DIR__.'/partials/footer.php';?>