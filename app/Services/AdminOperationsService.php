<?php

declare(strict_types=1);

final class AdminOperationsService
{
    public function __construct(private PDO $db) {}

    public function transactionSearch(string $q='',string $status='',int $limit=100):array{$limit=max(1,min(200,$limit));$where=[];$params=[];if($q!==''){$where[]='(t.reference LIKE ? OR t.description LIKE ? OR u.email LIKE ? OR a.account_number LIKE ?)';$like='%'.$q.'%';array_push($params,$like,$like,$like,$like);}if($status!==''){$where[]='t.status=?';$params[]=$status;}$sql='SELECT t.reference,t.type,t.status,t.amount,t.currency,t.description,t.created_at,t.completed_at,t.initiated_by,u.email,MAX(a.account_number) account_number FROM transactions t LEFT JOIN users u ON u.id=t.initiated_by LEFT JOIN ledger_entries le ON le.transaction_id=t.id LEFT JOIN accounts a ON a.id=le.account_id'.($where?' WHERE '.implode(' AND ',$where):'').' GROUP BY t.id,t.reference,t.type,t.status,t.amount,t.currency,t.description,t.created_at,t.completed_at,t.initiated_by,u.email ORDER BY t.id DESC LIMIT '.$limit;$stmt=$this->db->prepare($sql);$stmt->execute($params);return $stmt->fetchAll();}
    public function transaction(string $reference):?array{$stmt=$this->db->prepare('SELECT t.*,u.email FROM transactions t LEFT JOIN users u ON u.id=t.initiated_by WHERE t.reference=? LIMIT 1');$stmt->execute([$reference]);$t=$stmt->fetch();if(!$t)return null;$stmt=$this->db->prepare('SELECT le.*,a.account_number,a.user_id FROM ledger_entries le JOIN accounts a ON a.id=le.account_id WHERE le.transaction_id=? ORDER BY le.id');$stmt->execute([(int)$t['id']]);$t['ledger']=$stmt->fetchAll();$stmt=$this->db->prepare('SELECT te.*,u.email FROM transaction_events te LEFT JOIN users u ON u.id=te.actor_user_id WHERE te.transaction_id=? ORDER BY te.id DESC');$stmt->execute([(int)$t['id']]);$t['events']=$stmt->fetchAll();return $t;}

    public function requestAdjustment(string $reference,int $adminId,string $reason,string $operation):int{return $this->requestAction($operation,'transaction',$this->transactionId($reference),$adminId,$reason);}
    public function approveAction(int $requestId,int $adminId,string $decisionReason=''):string{$this->db->beginTransaction();try{$stmt=$this->db->prepare('SELECT * FROM admin_action_requests WHERE id=? FOR UPDATE');$stmt->execute([$requestId]);$r=$stmt->fetch();if(!$r||$r['status']!=='pending')throw new RuntimeException('Approval request is not pending.');if((int)$r['requested_by']===$adminId)throw new RuntimeException('Maker-checker rule: the requester cannot approve their own action.');if($r['action_type']==='reversal'||$r['action_type']==='refund'){$this->adjustTransactionLocked((int)$r['entity_id'],$adminId,$r['reason'],$r['action_type']);}$this->db->prepare('UPDATE admin_action_requests SET status="approved",approved_by=?,decision_reason=?,decided_at=CURRENT_TIMESTAMP WHERE id=?')->execute([$adminId,trim($decisionReason)?:null,$requestId]);$this->audit($adminId,'admin_action_approved','admin_action_request',$requestId,['action_type'=>$r['action_type'],'entity_id'=>$r['entity_id']]);$this->db->commit();return $r['action_type'];}catch(Throwable $e){if($this->db->inTransaction())$this->db->rollBack();throw $e;}}
    public function rejectAction(int $requestId,int $adminId,string $reason):void{$reason=trim($reason);if($reason==='')throw new InvalidArgumentException('A rejection reason is required.');$stmt=$this->db->prepare('SELECT requested_by,status FROM admin_action_requests WHERE id=?');$stmt->execute([$requestId]);$r=$stmt->fetch();if(!$r||$r['status']!=='pending')throw new RuntimeException('Approval request is not pending.');if((int)$r['requested_by']===$adminId)throw new RuntimeException('Requester cannot reject their own action.');$this->db->prepare('UPDATE admin_action_requests SET status="rejected",approved_by=?,decision_reason=?,decided_at=CURRENT_TIMESTAMP WHERE id=?')->execute([$adminId,$reason,$requestId]);$this->audit($adminId,'admin_action_rejected','admin_action_request',$requestId,['reason'=>$reason]);}
    public function pendingActions():array{$stmt=$this->db->query('SELECT ar.*,rq.email requester_email,ap.email approver_email FROM admin_action_requests ar JOIN users rq ON rq.id=ar.requested_by LEFT JOIN users ap ON ap.id=ar.approved_by WHERE ar.status="pending" ORDER BY ar.id DESC');return $stmt->fetchAll();}
    public function accounts(string $q='',int $limit=200):array{$limit=max(1,min(500,$limit));$like='%'.$q.'%';$stmt=$this->db->prepare('SELECT a.id,a.account_number,a.available_balance,a.status,at.name account_type,at.currency,u.id user_id,u.first_name,u.last_name,u.email FROM accounts a JOIN account_types at ON at.id=a.account_type_id JOIN users u ON u.id=a.user_id WHERE a.account_number LIKE ? OR u.email LIKE ? OR u.first_name LIKE ? OR u.last_name LIKE ? ORDER BY a.id DESC LIMIT '.$limit);$stmt->execute([$like,$like,$like,$like]);return $stmt->fetchAll();}
    public function accountTransactions(int $accountId,int $limit=100):array{$limit=max(1,min(200,$limit));$stmt=$this->db->prepare('SELECT t.reference,t.type,t.status,t.amount,t.currency,t.description,t.created_at,le.entry_type FROM ledger_entries le JOIN transactions t ON t.id=le.transaction_id WHERE le.account_id=? ORDER BY le.id DESC LIMIT '.$limit);$stmt->execute([$accountId]);return $stmt->fetchAll();}
    public function setAccountStatus(int $accountId,string $status,int $adminId):void{if(!in_array($status,['active','frozen','closed'],true))throw new InvalidArgumentException('Invalid account status.');$stmt=$this->db->prepare('UPDATE accounts SET status=? WHERE id=?');$stmt->execute([$status,$accountId]);if($stmt->rowCount()===0)throw new RuntimeException('Account not found or unchanged.');$this->audit($adminId,'account_status_changed','account',$accountId,['status'=>$status]);}
    public function customerSearch(string $q='',int $limit=100):array{$limit=max(1,min(200,$limit));$stmt=$this->db->prepare('SELECT u.id,u.first_name,u.last_name,u.email,u.status,r.name role,COUNT(DISTINCT a.id) accounts,COALESCE(SUM(a.available_balance),0) balance FROM users u LEFT JOIN roles r ON r.id=u.role_id LEFT JOIN accounts a ON a.user_id=u.id WHERE (u.first_name LIKE ? OR u.last_name LIKE ? OR u.email LIKE ?) GROUP BY u.id ORDER BY u.id DESC LIMIT '.$limit);$like='%'.$q.'%';$stmt->execute([$like,$like,$like]);return $stmt->fetchAll();}
    public function setCustomerStatus(int $userId,string $status,int $adminId):void{if(!in_array($status,['active','suspended','locked'],true))throw new InvalidArgumentException('Invalid customer status.');$stmt=$this->db->prepare('UPDATE users SET status=? WHERE id=? AND role_id=(SELECT id FROM roles WHERE name="customer")');$stmt->execute([$status,$userId]);if($stmt->rowCount()===0)throw new RuntimeException('Customer not found or unchanged.');$this->audit($adminId,'customer_status_changed','user',$userId,['status'=>$status]);}
    public function transferLimits(string $currency='USD'):array{$stmt=$this->db->prepare('SELECT * FROM admin_transfer_limits WHERE currency=?');$stmt->execute([$currency]);return $stmt->fetch()?:['currency'=>$currency,'daily_limit'=>'5000000.0000','per_transaction_limit'=>'1000000.0000'];}
    public function updateTransferLimits(string $currency,string $daily,string $perTransaction,int $adminId):void{$this->positive($daily);$this->positive($perTransaction);$stmt=$this->db->prepare('INSERT INTO admin_transfer_limits(currency,daily_limit,per_transaction_limit,updated_by) VALUES(?,?,?,?) ON DUPLICATE KEY UPDATE daily_limit=VALUES(daily_limit),per_transaction_limit=VALUES(per_transaction_limit),updated_by=VALUES(updated_by)');$stmt->execute([$currency,$daily,$perTransaction,$adminId]);$this->audit($adminId,'transfer_limits_updated','transfer_limit',0,['currency'=>$currency,'daily_limit'=>$daily,'per_transaction_limit'=>$perTransaction]);}
    public function latestReconciliation():?array{$stmt=$this->db->query('SELECT * FROM reconciliation_runs ORDER BY id DESC LIMIT 1');return $stmt->fetch()?:null;}
    public function reconciliationMismatches():array{$latest=$this->latestReconciliation();if(!$latest||empty($latest['details']))return [];return json_decode($latest['details'],true,512,JSON_THROW_ON_ERROR)?:[];}
    public function recentAuditEvents(int $limit=100):array{$limit=max(1,min(200,$limit));$stmt=$this->db->query('SELECT al.*,u.email FROM audit_logs al LEFT JOIN users u ON u.id=al.user_id ORDER BY al.id DESC LIMIT '.$limit);return $stmt->fetchAll();}

    private function requestAction(string $action,string $entityType,int $entityId,int $adminId,string $reason):int{$reason=trim($reason);if($reason==='')throw new InvalidArgumentException('A reason is required.');$stmt=$this->db->prepare('INSERT INTO admin_action_requests(action_type,entity_type,entity_id,requested_by,reason) VALUES(?,?,?,?,?)');$stmt->execute([$action,$entityType,$entityId,$adminId,$reason]);$id=(int)$this->db->lastInsertId();$this->audit($adminId,'admin_action_requested','admin_action_request',$id,['action_type'=>$action,'entity_id'=>$entityId,'reason'=>$reason]);return $id;}
    private function transactionId(string $reference):int{$stmt=$this->db->prepare('SELECT id FROM transactions WHERE reference=?');$stmt->execute([$reference]);$id=$stmt->fetchColumn();if(!$id)throw new RuntimeException('Transaction not found.');return (int)$id;}
    private function adjustTransactionLocked(int $transactionId,int $adminId,string $reason,string $operation):string{$stmt=$this->db->prepare('SELECT * FROM transactions WHERE id=? FOR UPDATE');$stmt->execute([$transactionId]);$original=$stmt->fetch();if(!$original||$original['status']!=='completed')throw new RuntimeException('Only completed transactions can be adjusted.');$field=$operation==='reversal'?'reversal_of_transaction_id':'refund_of_transaction_id';if(!empty($original[$field]))throw new RuntimeException('Transaction already adjusted.');$stmt=$this->db->prepare('SELECT le.* FROM ledger_entries le WHERE le.transaction_id=? ORDER BY le.id FOR UPDATE');$stmt->execute([$transactionId]);$entries=$stmt->fetchAll();if(!$entries)throw new RuntimeException('Transaction has no ledger entries.');$accountIds=array_values(array_unique(array_map(fn($e)=>(int)$e['account_id'],$entries)));sort($accountIds,SORT_NUMERIC);foreach($accountIds as $id){$lock=$this->db->prepare('SELECT id,status FROM accounts WHERE id=? FOR UPDATE');$lock->execute([$id]);$a=$lock->fetch();if(!$a||$a['status']!=='active')throw new RuntimeException('All affected accounts must be active.');}$newRef=strtoupper($operation).'-'.date('YmdHis').'-'.strtoupper(bin2hex(random_bytes(4)));$stmt=$this->db->prepare("INSERT INTO transactions(reference,type,status,amount,currency,description,initiated_by,$field) VALUES(?,?,\"processing\",?,?,?,?,?)");$stmt->execute([$newRef,$operation,$original['amount'],$original['currency'],ucfirst($operation).' of '.$original['reference'].': '.$reason,$adminId,$transactionId]);$newId=(int)$this->db->lastInsertId();foreach($entries as $e){$this->db->prepare('INSERT INTO ledger_entries(transaction_id,account_id,entry_type,amount) VALUES(?,?,?,?)')->execute([$newId,(int)$e['account_id'],$e['entry_type']==='credit'?'debit':'credit',$e['amount']]);}$this->db->prepare('UPDATE transactions SET status="completed",completed_at=CURRENT_TIMESTAMP WHERE id=?')->execute([$newId]);$this->db->prepare("UPDATE transactions SET $field=?,status=? WHERE id=?")->execute([$newId,$operation==='reversal'?'reversed':'refunded',$transactionId]);foreach($accountIds as $id){$stmt=$this->db->prepare('SELECT COALESCE(SUM(CASE WHEN le.entry_type="credit" THEN le.amount ELSE -le.amount END),0) FROM ledger_entries le JOIN transactions t ON t.id=le.transaction_id WHERE le.account_id=? AND t.status="completed"');$stmt->execute([$id]);$this->db->prepare('UPDATE accounts SET available_balance=? WHERE id=?')->execute([number_format((float)$stmt->fetchColumn(),4,'.',''),$id]);}$this->recordEventById($transactionId,$operation.'_completed','completed',$operation.'d',$adminId,$reason);$this->recordEventById($newId,$operation.'_created','processing','completed',$adminId,$reason);$this->audit($adminId,'transaction_'.$operation,'transaction',$transactionId,['reference'=>$original['reference'],'compensating_reference'=>$newRef,'reason'=>$reason]);return $newRef;}
    private function recordEventById(int $transactionId,string $type,string $old,string $new,int $actor,string $reason):void{$this->db->prepare('INSERT INTO transaction_events(transaction_id,event_type,old_status,new_status,actor_user_id,metadata) VALUES(?,?,?,?,?,?)')->execute([$transactionId,$type,$old,$new,$actor,json_encode(['reason'=>$reason],JSON_THROW_ON_ERROR)]);}
    private function audit(int $userId,string $action,string $entityType,int $entityId,array $details):void{$this->db->prepare('INSERT INTO audit_logs(user_id,action,entity_type,entity_id,ip_address,details) VALUES(?,?,?,?,?,?)')->execute([$userId,$action,$entityType,$entityId,$_SERVER['REMOTE_ADDR']??null,json_encode($details,JSON_THROW_ON_ERROR)]);}
    private function positive(string $v):void{if(!preg_match('/^\d+(\.\d{1,4})?$/',$v)||(float)$v<=0)throw new InvalidArgumentException('Limit must be greater than zero.');}
}
